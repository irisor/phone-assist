# Save exact versions (no ^ or ~ ranges)
save-exact=true

# Disable automatic execution of install scripts (security)
ignore-scripts=true

# Always generate package-lock.json
package-lock=true

# Use npm ci in CI/CD for deterministic builds
engine-strict=true
